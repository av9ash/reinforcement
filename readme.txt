The actions are,

execute_careful/normal_place(book_name,bin_name,current_state)
execute_careful/normal_pick(book_name,current_state)
execute_careful/normal_moveF/TurnCCW/TurnCW()

current_state is instance of State class
all the action return success_code(1/-1),next_state(string),reward.
